<!DOCTYPE html>
<html>
<head>

  <!-- Meta Tags -->
  <%= render "layouts/components/tags" %>

  <!-- Styles -->
  <%= render "layouts/components/styles" %>

  <!-- Tracking Pixels -->
  <%= render "layouts/components/tracking_head" %>

</head>
<body class="cx cx-main theme-default_mixed beach-theme cx-listings_details">

  <!-- Content -->
  <main>
    <%= yield %>
  </main>

  <!-- Scripts -->
  <%= render "layouts/components/scripts" %>

</body>
</html>
